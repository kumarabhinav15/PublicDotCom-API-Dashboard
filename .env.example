# 1) Mock mode (safe default)
MOCK_PUBLIC_API=true

# 2) Live mode (server-side only; use with caution)
# MOCK_PUBLIC_API=false
# PUBLIC_SECRET_TOKEN=your_public_personal_secret_token_here
# PUBLIC_API_BASE_URL=https://api.public.com
# PUBLIC_ACCESS_TOKEN_TTL_MINUTES=15

# Prisma (optional; the starter does not require the DB yet)
DATABASE_URL="file:./dev.db"

# Trading safety gates (server-side)
# ENABLE_TRADING=false
# TRADING_UNLOCK_CODE=choose-a-secret
